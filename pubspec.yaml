name: tap
description: Tap test given by Hasan Abbas Sorathiya

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  dio:
  provider:
  flutter_staggered_grid_view:
  carousel_slider:
  chewie:
  video_player:


dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:

  uses-material-design: true
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg





